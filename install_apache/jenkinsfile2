#!groovy
// Check hp (or IP address) properties
properties([disableConcurrentBuilds()]) // Запретить одновременные задачи

pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                sh '''
                  pwd
                  cd install_apache
                  pwd
                  ls -la /home/bazil/.ssh
                '''
            }
        }
    }
}